name: Smoke

on:
  push:
  workflow_dispatch:

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Print deterministic message
        run: echo "SMOKE_OK_INFOLCA_UNIVERSALREPO_2026_04_26"
